# NOTE: pnpm 10+ reads its settings from pnpm-workspace.yaml, NOT from this file. The real
# setting is `nodeLinker: hoisted` there; the line below is kept only for older pnpm and has no
# effect under the pinned pnpm 11. Verify with `pnpm config get node-linker` before trusting it.
node-linker=hoisted
